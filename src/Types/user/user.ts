export type TUser = {
  email: string;
  exp: number;
  iat: number;
  role: string;
};
export type TAllUserData = {
  _id: string;
  name: string;
  email: string;
  phone: number;
  role: string;
  address: string;
};
